apiVersion: v1
kind: Service
metadata:
  labels:
    service: dash
  name: dash
  namespace: default
spec:
  ports:
    - name: "8000"
      port: 8000
      targetPort: 8000
  selector:
    service: dash
status:
  loadBalancer: {}
